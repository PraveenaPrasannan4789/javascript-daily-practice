A HashMap is a data structure that stores data in key → value pairs.
#  Think of it like a dictionary:

name → "Praveena"
age  → 25
city → "London"

key = unique identifier

value = data stored
# In JavaScript

HashMap is implemented using:

## Map (recommended)
or
## plain Object

HashMap allows constant time lookup O(1), which improves efficiency”