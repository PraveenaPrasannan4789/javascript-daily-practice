// Creating object
const user = {
  name: "Praveena",
  age: 24
};

// Accessing properties
console.log(user.name);
console.log(user["age"]);

// Adding property
user.city = "London";

// Deleting property
delete user.age;